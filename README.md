## Chat on terminal with Socket.io

For use, run `node server.js` to be a server,
before run `node terminal.js` and enter your chat url.
Default is `http://localhost:3000`


### Using:

- express
- socket.io
- readline